# Frontend (Next.js) — copy to .env.local
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
# Must be API port (8000), NOT dashboard port (3000)
NEXT_PUBLIC_DEV_HINTS=1
NEXT_PUBLIC_FOUNDER_ANALYTICS=1
# Active project is chosen in the dashboard (localStorage). Do not use NEXT_PUBLIC_PROJECT_ID.

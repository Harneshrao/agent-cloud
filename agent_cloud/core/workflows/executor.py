"""Workflow execution orchestration — call into agent runtime from app/execution."""

from __future__ import annotations

# Orchestration bridges `engine.dag_scheduler` during migration.
def placeholder() -> None:
    return None

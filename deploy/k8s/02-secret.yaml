# Edit stringData before apply. Do not commit real production secrets.
apiVersion: v1
kind: Secret
metadata:
  name: agent-cloud-secrets
  namespace: agent-cloud
type: Opaque
stringData:
  POSTGRES_PASSWORD: "changeme"
  DATABASE_URL: "postgresql://agent:changeme@postgres:5432/agent_cloud"

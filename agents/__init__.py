"""
Top-level package for filesystem-based agents.

Each subdirectory should contain an `agent.py` implementing an AgentBase
subclass plus optional configuration files (e.g. config.yaml, README.md).
"""

